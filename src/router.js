// Using statements.
// const controllers = require('./controllers');
// const middleware = require('./middleware');

// Router to export.
const router = (app) => {
    
};

// Export the router.
module.exports = router;